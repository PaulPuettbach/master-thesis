apiVersion: v1
kind: Pod
metadata:
  name: s3-test-pod
  labels:
    app: s3-test
spec:
  containers:
  - name: awscli
    image: amazon/aws-cli
    command: ["sleep", "3600"]
    env:
      - name: AWS_ACCESS_KEY_ID
        value: "minio"
      - name: AWS_SECRET_ACCESS_KEY
        value: "minio123"
