apiVersion: v1
kind: Pod
metadata:
  name: dns-test-pod
  labels:
    app: dns-test
spec:
  containers:
  - name: dnsutils
    image: busybox
    command: ["sleep", "3600"]
#kubectl run -it --rm --image=curlimages/curl curly -- /bin/sh